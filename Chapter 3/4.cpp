#include <iostream>
#include <algorithm>
#include <cstring>
#include <string>
#include <cmath>
#include <functional>
#include <vector>
#include <stack>
#include <queue>
#include <map>
#include <set>
#define MAX 51
//#define INF 1000000000
using namespace std;
//typedef long long int LL;

char a[MAX * 2][MAX];

int f()
{
}

int main(int agrc, char *agrv[])
{
    int n, m;
    while (~scanf("%d %d", &n, &m))
    {
        for (int i = 0; i < m; i++)
        {
            scanf("%s", a[i]);
        }
    }
}